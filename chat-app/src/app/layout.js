export const metadata = {
  title: 'Live Chat',
  description: 'Developed by Binesh Poudel',
}
import './globals.css'
export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  )
}
